// Import all of Bootstrap's JS
import * as bootstrap from 'bootstrap'

import AOS from "aos";
// import "../node_modules/aos/dist/aos.css";

// init AOS animation
AOS.init({
    duration: 1000,
    offset: 120,
});

